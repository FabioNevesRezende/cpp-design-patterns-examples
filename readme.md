# C++ Design patters Examples

#### To build each file individually call CMake with the option for the name of the pattern, example:

    cmake -DFACTORY=1 --fresh # will build only the factory pattern
    make 